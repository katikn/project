<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liveum</title>
    <link rel="stylesheet" href="/styles/main.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>

<body>
    {{> header}}
    <div class="container">
        <div class='row justify-content-md-center'>
            <form action="/editpost" method="POST" enctype="multipart/form-data">
                {{#each querys}}
                <div class="mb-3">
                    <span>Заголовок</span>
                    <input type="text" name="title" id="title" value="{{title}}" class="form-control"
                        placeholder="Впишите загаловок">
                </div>
                <div class="mb-3">
                    <span>Текст</span>
                    <textarea name="text" id="text" placeholder="Впишите текст" class="form-control" rows="5"
                        cols="35">{{text}}</textarea>
                </div>
                <div class="mb-3">
                    <span>Категория поста: {{category}}. {{name}}</span> <br>
                    <span>Выберите категорию:</span>
                    <select name="select" class="form-control" name="category" required>
                        <option value="1">1. popular</option>
                        <option value="2">2. travel</option>
                        <option value="3">3. covid</option>
                        <option value="4">4. technology</option>
                        <option value="5">5. education</option>
                        <option value="6">6. life</option>
                    </select>
                </div>
                <div class="mb-3">
                    <img width="190px" src="/images/{{img}}" alt="">
                    <input type="file" name="image" accept="image/*, image/jpeg" required>
                </div>
                <input type="submit" class="btn btn-primary" value="Edit">
                <input type="hidden" name="id" id="id" value="{{id}}">
                {{/each}}
            </form>
        </div>
    </div>
</body>

</html>